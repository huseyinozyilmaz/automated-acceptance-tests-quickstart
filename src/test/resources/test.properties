# Properties can be overwritten at runtime by passing in maven arguments using -D
# e.g. mvn clean verify -Dapplication.base.url=http://localhost:8080

application.base.url=http://localhost:3000
selenium.wait.timeout.seconds=10

login.valid.username=valid@email.com
login.valid.password=secret